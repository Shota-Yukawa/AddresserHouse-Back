spring:
  datasource:
    url:  jdbc:postgresql://ah-postgres.cotg2onn7xlt.ap-northeast-1.rds.amazonaws.com/residence
    username: residence
    password: residence
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      use_sql_comments: true
    properties:
      hibernate:
        format_sql: true
  jackson:
   time-zone: Asia/Tokyo

server:
  port: 8081
  servlet:
    context-path: /residence
  
logging:
  level:
    org: 
      hibernate:
        SQL: debug
      type:
        descriptor:
          sql: trace
      springframework:
        orm:
          jpa: DEBUG
